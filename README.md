# detection-fuites-pfe
on va travaillez ici ensemble

## Firebase Cloud Messaging

Les push mobiles utilisent Firebase Admin. Sur Render, definir la variable d'environnement `FIREBASE_SERVICE_ACCOUNT_JSON` avec le JSON du compte de service Firebase.
